#include <stdio.h>
#include <stdlib.h>
#include <iostream>
using namespace std;


void menu()
{
    int *A,n,ch,x;
    cout << "Enter the dimension";
    cin >> n;
    A = new int[n];

    do
    {
        // Display
        switch(ch)
        {
            case 1:
            for(i=1;i<=n;i++)
            {
                cin >> A[i-1];
            }
            break;
            case 2;
            cin >> 
        

        }
    } while (condition);
    

}